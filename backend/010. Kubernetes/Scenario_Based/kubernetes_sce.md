Cluster Ip - Internal-only services reachable inside the cluster.
ConfigMap - Stores non-sensitive configuration data.
Container - Application runtime package executed inside pods.
Controller Manager -  Enforces desired state by watching and correcting drift.
Cron Job - Runs Scheduled or recurring Jobs
DaemonSet - Ensures a pon runs on every node or specific nodes
Deployment - Manage 