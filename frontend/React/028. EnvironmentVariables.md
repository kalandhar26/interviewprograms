# Environment Variables
- Imagine you have different houses 🏡: One for school homework, One for weekend fun
- You don’t want to carry the same keys everywhere 🔑 , School key → only for school, Weekend key → only for weekend
- In React: API URLs, secrets, feature flags, Change depending on environment (dev, staging, production), 
- Instead of hardcoding, you use environment variables. React automatically swaps them based on .env files


## Professional Definition
- Environment Variables are external configuration values that store environment-specific information (API endpoints, secrets, feature flags) in React applications, typically using .env files, allowing safe and flexible app configuration across development, staging, and production environments.

